# 002_SHA384.pdf

## Page 1

The SHA384 hash implements the HashFunction protocol for the specific case of SHA-2 hashi
with a 384-bit digest (SHA384Digest). Larger digests take more space but are more secure.
You can compute the digest by calling the static hash(data:) method once. Alternatively, if the
data that you want to hash is too large to fit in memory, you can compute the digest iteratively by
creating a new hash instance, calling the update(data:) method repeatedly with blocks of dat
and then calling the finalize() method to get the result.
typealias Digest
The digest type for a SHA384 hash function.
struct SHA384Digest
The output of a Secure Hashing Algorithm 2 (SHA-2) hash with a 384-bit digest.
Overview
Topics
Specifying the output type
Apple CryptoKit / SHA384
Structure
SHA384
An implementation of Secure Hashing Algorithm 2 (SHA-2) hashing with a 384-b
digest.
iOS 13.0+
iPadOS 13.0+
Mac Catalyst 13.0+
macOS 10.15+
tvOS 13.0+
visionOS 1.0+
watchOS 6.0+


## Page 2

init()
Creates a SHA384 hash function.
func update(bufferPointer: UnsafeRawBufferPointer)
Incrementally updates the hash function with the contents of the buffer.
func finalize() -> SHA384.Digest
Finalizes the hash function and returns the computed digest.
static let byteCount: Int
The number of bytes in a SHA384 digest.
static let blockByteCount: Int
The number of bytes that represents the hash function’s internal state.
Copyable
HashFunction
Sendable
SendableMetatype
protocol HashFunction
A type that performs cryptographically secure hashing.
Computing a hash iteratively
Inspecting hash information
Relationships
Conforms To
See Also
Cryptographically secure hashes


## Page 3

struct SHA512
An implementation of Secure Hashing Algorithm 2 (SHA-2) hashing with a 512-bit digest.
struct SHA256
An implementation of Secure Hashing Algorithm 2 (SHA-2) hashing with a 256-bit digest.


